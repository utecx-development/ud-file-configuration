package de.ufomc;

import java.io.IOException;

public class Main {
    public static void main(String[] args) {

        /*Webserver server = new Webserver(80);

        server.addRoute("/", response -> {
            try {
                File file = new File("D:\\profile\\index.html");
                response.sendFile("200 OK", "text/html", file);
            } catch (IOException e) {
                e.printStackTrace();
                response.sendResponse("500 Internal Server Error", "text/html", "Error loading file");
            }
        });

        server.start();*/
    }
}